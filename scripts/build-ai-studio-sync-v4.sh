#!/usr/bin/env bash
set -euo pipefail

printf '%s\n' 'AI Studio v4 is a retired client-only archive and must not be rebuilt or imported.'
printf '%s\n' 'Create a new archive only after the canonical AI Studio app has an audited full-stack server runtime.'
exit 1
