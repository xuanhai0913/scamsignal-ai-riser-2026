# Required. AI Studio injects this server-side from the app's Secrets panel.
GEMINI_API_KEY=""

# Optional runtime variables are intentionally omitted here so AI Studio does not
# block preview/share while waiting for values that already have safe defaults:
# GEMINI_MODEL, WEB_RISK_API_KEY, VITE_API_BASE_URL, PORT and APP_URL.
